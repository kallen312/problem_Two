//Declare a function, named plusTwo  that accepts one parameter, a number.
//Have the function return the number increased by 2.

function plusTwo(num) {
  let newNum = num * 2;
  return newNum;
}

console.log(plusTwo(18));
